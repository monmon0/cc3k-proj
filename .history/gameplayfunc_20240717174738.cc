#include <stdlib.h>

class RandomPos() {
    int x;
    int y;
//  
    public:
        RandomPos() {
            x = 1+ (rand() % 100). ;
            y = 0;
        };
        int getX();
        int getY();
}